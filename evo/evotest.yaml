ripple-image: "ghcr.io/amousavigourabi/docker-rippled/seeded-2.4.0-fully-lowered-threshold:latest"

seed: 98

max_generation: 5
population_size: 3

encoding:
  # Delay
  min_value: 0 # Minimum delay value in milliseconds
  max_value: 4000 # Maximum delay value in milliseconds
